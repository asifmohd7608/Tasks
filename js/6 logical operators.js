let x = 4;
let y = 4;
let z = 6;

if (x == y && x < z) {
  // and
  console.log("x is same as y and less than z");
}
if (x == y || x > z) {
  // or
  console.log("either x is same as y or x greater than z");
}
if (x != z) {
  //not
  console.log("x is not same as z");
}
